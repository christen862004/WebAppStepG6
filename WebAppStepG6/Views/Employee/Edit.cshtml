@model EmpWithDeptListViewModel
@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>
<!--/employee/SaveEdit/1?name=&Salary=&ImageURL=&DepartmentID=-->
<form method="post" action="/employee/SaveEdit/@Model.EmpId">
    <div>
      @*   <label>Name</label>
        <input type="text" id="EmpName" name="EmpName" value="@Model.EmpName" class="form form-control" /> *@
        @* @Html.TextBoxFor(e => e.EmpName, new { style = "color:red",@class="form form-control"}) *@
        @* @Html.LabelFor(e => e.EmpName)
        @Html.EditorFor(e=>e.EmpName) *@

        <label asp-for="EmpName"></label>
        <input class="form form-control" asp-for="EmpName" />
    </div>
    <div>
        <label>Salary</label>
        <input type="number" id="Salary" name="Salary" value="@Model.Salary" class="form form-control" />
    </div>
    <div>
        <label>ImageUrl</label>
        <input type="text" id="ImageUrl" name="ImageUrl" value="@Model.ImageUrl" class="form form-control" />
    </div>
    <div>
        <label>DepartmentId</label>
        <select id="DepartmentId" name="DepartmentId"  class="form form-control" >
            @foreach (var deptItem in Model.DeptList)
            {
                if (Model.DepartmentId == deptItem.Id)
                {
                    <option value="@deptItem.Id" selected>@deptItem.Name</option>
                }
                else
                {
                    <option value="@deptItem.Id" >@deptItem.Name</option>

                }
            }
        </select>
    </div>
    <input type="submit" value="Save" class="btn btn-primary" />

</form>
<a href="/Employee/Index">Back to List</a>
|@Html.ActionLink("- Back -",actionName:"Index",controllerName:"Employee")
|<a asp-action="Index" asp-controller="Employee">Back to list</a>
<hr />
<a href="/Employee/Details/@Model.EmpId?name=@Model.EmpName">Details</a>
@Html.ActionLink("- Details -",actionName:"Details",controllerName:"Employee"
,routeValues:new{id=Model.EmpId,name=Model.EmpName})

|<a asp-action="Details" asp-controller="Employee"
    asp-route-id="@Model.EmpId" asp-route-name="@Model.EmpName">Details</a>

